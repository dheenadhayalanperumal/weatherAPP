<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.INTERNET"/>
  <!-- Foreground location for the "use my current location" search. COARSE is
       the one that matters: weather is regional and the app truncates
       coordinates to ~1km anyway. FINE is declared because Android grants
       COARSE-only through the same dialog and some devices will not surface
       the precise/approximate choice without it. -->
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
  <!-- Removed: SYSTEM_ALERT_WINDOW, READ_EXTERNAL_STORAGE and
       WRITE_EXTERNAL_STORAGE. No code in this app draws overlays or touches
       external storage. SYSTEM_ALERT_WINDOW is already declared in
       src/debug/AndroidManifest.xml where the dev-client actually needs it;
       the copy here shipped it to production, where Play Console flags it on
       review and the storage permissions trigger a declaration requirement. -->
  <uses-permission android:name="android.permission.VIBRATE"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:allowBackup="true" android:theme="@style/AppTheme" android:supportsRtl="true" android:enableOnBackInvokedCallback="false">
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="false"/>
    <meta-data android:name="expo.modules.updates.ENABLE_BSDIFF_PATCH_SUPPORT" android:value="true"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode|smallestScreenSize|assetsPaths" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <!-- Was "yourapp", the Expo template placeholder, so the
             "trueweather" scheme declared in app.json resolved on neither
             platform. -->
        <data android:scheme="trueweather"/>
        <data android:scheme="com.dheena007.weatherapp"/>
      </intent-filter>
    </activity>
  </application>
</manifest>